# env: "local"
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8082"
  timeout: 4s
  # время жизни соединения с клиентом, чтобы не создавать новое соединение для того же клиента
  idle_timeout: 60s
  user: alan
